spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME:}
    password: ${DB_PASSWORD:}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: ${SERVER_PORT:8040}

logging:
  level:
    root: INFO
    com.apargo.waba: DEBUG
    org.hibernate.SQL: DEBUG